pipeline {
    agent any

    options {
        skipDefaultCheckout()
    }
    /*stages {
        stage('Clean') {
            steps {
                sh "mvn clean"
            }
        }
        stage('Test') {
            steps {
                sh "mvn test"
            }
        }
        stage('Deploy') {
            steps {
                sh "mvn package"
            }
        }
    }*/
    stages {
        stage('GitHub'){
            steps {
                echo "TEST"
            }
        }
    }
}
